name: live_location_tracker_plus_example
description: "Demonstrates how to use the live_location_tracker_plus plugin."
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  live_location_tracker_plus:
    path: ../

  google_maps_flutter: ^2.10.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
